#!/bin/sh


# ~/.xinitrc runs automatically when you run startx

# if a login screen is used, ~/.xprofile is run rather than ~/.xinitrc


. "$HOME/.xprofile"     # . means to source the file from the current shell
